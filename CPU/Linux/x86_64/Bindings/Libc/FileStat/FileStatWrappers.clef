module Fidelity.Libc.FileStat.Wrappers

// Generated by Farscape — c

open Fidelity.Libc.FileStat
open Fidelity.Libc.Errno
open Microsoft.FSharp.NativeInterop

    /// Capture errno and build CError with description from header comments.
    let captureError () : CError =
        let code = NativePtr.read (Fidelity.Libc.FileStat.__errno_location ())
        { Code = code; Description = Errno.describe code }



    /// Test for access to NAME using the real UID and real GID.
    /// 
    /// C signature: int access(const char * __name, int __type)
    let access (name: nativeptr<byte>) (``type``: int32) : Result<unit, CError> =
        let result = Fidelity.Libc.FileStat.access name ``type``
        if result = 0l then Ok ()
        else Error (captureError ())

    /// Make a link to FROM named TO.
    /// 
    /// C signature: int link(const char * __from, const char * __to)
    let link (from: nativeptr<byte>) (``to``: nativeptr<byte>) : Result<unit, CError> =
        let result = Fidelity.Libc.FileStat.link from ``to``
        if result = 0l then Ok ()
        else Error (captureError ())

    /// Like link but relative paths in TO and FROM are interpreted relative to FROMFD and TOFD respectively.
    /// 
    /// C signature: int linkat(int __fromfd, const char * __from, int __tofd, const char * __to, int __flags)
    let linkat (fromfd: int32) (from: nativeptr<byte>) (tofd: int32) (``to``: nativeptr<byte>) (flags: int32) : Result<unit, CError> =
        let result = Fidelity.Libc.FileStat.linkat fromfd from tofd ``to`` flags
        if result = 0l then Ok ()
        else Error (captureError ())

    /// Make a symbolic link to FROM named TO.
    /// 
    /// C signature: int symlink(const char * __from, const char * __to)
    let symlink (from: nativeptr<byte>) (``to``: nativeptr<byte>) : Result<unit, CError> =
        let result = Fidelity.Libc.FileStat.symlink from ``to``
        if result = 0l then Ok ()
        else Error (captureError ())

    /// Read the contents of the symbolic link PATH into no more than LEN bytes of BUF.  The contents are not null-terminated. Returns the number of characters read, or -1 for errors.
    /// 
    /// C signature: ssize_t readlink(const char *restrict __path, char *restrict __buf, size_t __len)
    let readlink (path: nativeptr<byte>) (buf: nativeptr<byte>) (len: unativeint) : Result<nativeint, CError> =
        let result = Fidelity.Libc.FileStat.readlink path buf len
        if result >= 0n then Ok result
        else Error (captureError ())

    /// Like symlink but a relative path in TO is interpreted relative to TOFD.
    /// 
    /// C signature: int symlinkat(const char * __from, int __tofd, const char * __to)
    let symlinkat (from: nativeptr<byte>) (tofd: int32) (``to``: nativeptr<byte>) : Result<unit, CError> =
        let result = Fidelity.Libc.FileStat.symlinkat from tofd ``to``
        if result = 0l then Ok ()
        else Error (captureError ())

    /// Like readlink but a relative PATH is interpreted relative to FD.
    /// 
    /// C signature: ssize_t readlinkat(int __fd, const char *restrict __path, char *restrict __buf, size_t __len)
    let readlinkat (fd: int32) (path: nativeptr<byte>) (buf: nativeptr<byte>) (len: unativeint) : Result<nativeint, CError> =
        let result = Fidelity.Libc.FileStat.readlinkat fd path buf len
        if result >= 0n then Ok result
        else Error (captureError ())

    /// Remove the link NAME.
    /// 
    /// C signature: int unlink(const char * __name)
    let unlink (name: nativeptr<byte>) : Result<unit, CError> =
        let result = Fidelity.Libc.FileStat.unlink name
        if result = 0l then Ok ()
        else Error (captureError ())

    /// Remove the directory PATH.
    /// 
    /// C signature: int rmdir(const char * __path)
    let rmdir (path: nativeptr<byte>) : Result<unit, CError> =
        let result = Fidelity.Libc.FileStat.rmdir path
        if result = 0l then Ok ()
        else Error (captureError ())

    /// Get file attributes for the file, device, pipe, or socket that file descriptor FD is open on and put them in BUF.
    /// 
    /// C signature: int fstat(int __fd, struct stat * __buf)
    let fstat (fd: int32) (buf: nativeint) : Result<unit, CError> =
        let result = Fidelity.Libc.FileStat.fstat fd buf
        if result = 0l then Ok ()
        else Error (captureError ())

    /// Get file attributes about FILE and put them in BUF. If FILE is a symbolic link, do not follow it.
    /// 
    /// C signature: int lstat(const char *restrict __file, struct stat *restrict __buf)
    let lstat (file: nativeptr<byte>) (buf: nativeint) : Result<unit, CError> =
        let result = Fidelity.Libc.FileStat.lstat file buf
        if result = 0l then Ok ()
        else Error (captureError ())

    /// Set file access permissions for FILE to MODE. If FILE is a symbolic link, this affects its target instead.
    /// 
    /// C signature: int chmod(const char * __file, __mode_t __mode)
    let chmod (file: nativeptr<byte>) (mode: uint32) : Result<unit, CError> =
        let result = Fidelity.Libc.FileStat.chmod file mode
        if result = 0l then Ok ()
        else Error (captureError ())

    /// C signature: int fchmod(int __fd, __mode_t __mode)
    let fchmod (fd: int32) (mode: uint32) : Result<unit, CError> =
        let result = Fidelity.Libc.FileStat.fchmod fd mode
        if result = 0l then Ok ()
        else Error (captureError ())

    /// Set the file creation mask of the current process to MASK, and return the old creation mask.
    /// 
    /// C signature: __mode_t umask(__mode_t __mask)
    let umask (mask: uint32) : uint32 =
        Fidelity.Libc.FileStat.umask mask

    /// Create a new directory named PATH, with permission bits MODE.
    /// 
    /// C signature: int mkdir(const char * __path, __mode_t __mode)
    let mkdir (path: nativeptr<byte>) (mode: uint32) : Result<unit, CError> =
        let result = Fidelity.Libc.FileStat.mkdir path mode
        if result = 0l then Ok ()
        else Error (captureError ())

    /// Like mkdir, create a new directory with permission bits MODE.  But interpret relative PATH names relative to the directory associated with FD.
    /// 
    /// C signature: int mkdirat(int __fd, const char * __path, __mode_t __mode)
    let mkdirat (fd: int32) (path: nativeptr<byte>) (mode: uint32) : Result<unit, CError> =
        let result = Fidelity.Libc.FileStat.mkdirat fd path mode
        if result = 0l then Ok ()
        else Error (captureError ())
