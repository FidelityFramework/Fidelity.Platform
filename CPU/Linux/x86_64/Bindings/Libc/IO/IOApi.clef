module Fidelity.Libc.IO.Api

// Generated by Farscape — c

open Fidelity.Libc.IO
open Fidelity.Libc.Errno
open Fidelity.Libc.Errno.Errno

    /// Capture errno as human-readable description string from header comments.
    let captureErrno () : string =
        describe (NativePtr.read (NativePtr.ofNativeInt (__errno_location ())))



    /// C signature: __off_t lseek(int __fd, __off_t __offset, int __whence)
    let lseek (fd: int) (offset: int64) (whence: int) : Result<int64, string> =
        let result = Fidelity.Libc.IO.lseek fd offset whence
        if result >= 0n then Ok result
        else Error (captureErrno ())

    /// Close the file descriptor FD. This function is a cancellation point and therefore not marked with __THROW.
    /// 
    /// C signature: int close(int __fd)
    let close (fd: int) : Result<unit, string> =
        let result = Fidelity.Libc.IO.close fd
        if result = 0 then Ok ()
        else Error (captureErrno ())

    /// Read NBYTES into BUF from FD.  Return the number read, -1 for errors or 0 for EOF. This function is a cancellation point and therefore not marked with __THROW.
    /// 
    /// C signature: ssize_t read(int __fd, void * __buf, size_t __nbytes)
    let read (fd: int) (buf: option<nativeint>) (nbytes: unativeint) : Result<nativeint, string> =
        let result = Fidelity.Libc.IO.read fd buf nbytes
        if result >= 0n then Ok result
        else Error (captureErrno ())

    /// Write N bytes of BUF to FD.  Return the number written, or -1. This function is a cancellation point and therefore not marked with __THROW.
    /// 
    /// C signature: ssize_t write(int __fd, const void * __buf, size_t __n)
    let write (fd: int) (buf: option<nativeint>) (n: unativeint) : Result<nativeint, string> =
        let result = Fidelity.Libc.IO.write fd buf n
        if result >= 0n then Ok result
        else Error (captureErrno ())

    /// Read NBYTES into BUF from FD at the given position OFFSET without changing the file pointer.  Return the number read, -1 for errors or 0 for EOF. This function is a cancellation point and therefore not marked with __THROW.
    /// 
    /// C signature: ssize_t pread(int __fd, void * __buf, size_t __nbytes, __off_t __offset)
    let pread (fd: int) (buf: option<nativeint>) (nbytes: unativeint) (offset: int64) : Result<nativeint, string> =
        let result = Fidelity.Libc.IO.pread fd buf nbytes offset
        if result >= 0n then Ok result
        else Error (captureErrno ())

    /// Write N bytes of BUF to FD at the given position OFFSET without changing the file pointer.  Return the number written, or -1. This function is a cancellation point and therefore not marked with __THROW.
    /// 
    /// C signature: ssize_t pwrite(int __fd, const void * __buf, size_t __n, __off_t __offset)
    let pwrite (fd: int) (buf: option<nativeint>) (n: unativeint) (offset: int64) : Result<nativeint, string> =
        let result = Fidelity.Libc.IO.pwrite fd buf n offset
        if result >= 0n then Ok result
        else Error (captureErrno ())

    /// Create a one-way communication channel (pipe). If successful, two file descriptors are stored in PIPEDES; bytes written on PIPEDES[1] can be read from PIPEDES[0]. Returns 0 if successful, -1 if not.
    /// 
    /// C signature: int pipe(int * __pipedes)
    let pipe (pipedes: option<nativeint>) : Result<unit, string> =
        let result = Fidelity.Libc.IO.pipe pipedes
        if result = 0 then Ok ()
        else Error (captureErrno ())

    /// Duplicate FD, returning a new file descriptor on the same file.
    /// 
    /// C signature: int dup(int __fd)
    let dup (fd: int) : Result<int, string> =
        let result = Fidelity.Libc.IO.dup fd
        if result >= 0 then Ok result
        else Error (captureErrno ())

    /// Duplicate FD to FD2, closing FD2 and making it open on the same file.
    /// 
    /// C signature: int dup2(int __fd, int __fd2)
    let dup2 (fd: int) (fd2: int) : Result<int, string> =
        let result = Fidelity.Libc.IO.dup2 fd fd2
        if result >= 0 then Ok result
        else Error (captureErrno ())

    /// Make all changes done to FD actually appear on disk. This function is a cancellation point and therefore not marked with __THROW.
    /// 
    /// C signature: int fsync(int __fd)
    let fsync (fd: int) : Result<unit, string> =
        let result = Fidelity.Libc.IO.fsync fd
        if result = 0 then Ok ()
        else Error (captureErrno ())

    /// Make all changes done to all files actually appear on disk.
    /// 
    /// C signature: void sync()
    let sync () : unit =
        Fidelity.Libc.IO.sync ()

    /// C signature: int ftruncate(int __fd, __off_t __length)
    let ftruncate (fd: int) (length: int64) : Result<unit, string> =
        let result = Fidelity.Libc.IO.ftruncate fd length
        if result = 0 then Ok ()
        else Error (captureErrno ())

    /// Synchronize at least the data part of a file with the underlying media.
    /// 
    /// C signature: int fdatasync(int __fildes)
    let fdatasync (fildes: int) : Result<unit, string> =
        let result = Fidelity.Libc.IO.fdatasync fildes
        if result = 0 then Ok ()
        else Error (captureErrno ())

    /// C signature: int fcntl(int __fd, int __cmd)
    let fcntl (fd: int) (cmd: int) : Result<int, string> =
        let result = Fidelity.Libc.IO.fcntl fd cmd
        if result >= 0 then Ok result
        else Error (captureErrno ())

    /// C signature: int open(const char * __file, int __oflag)
    let open (file: option<nativeptr<byte>>) (oflag: int) : Result<int, string> =
        let result = Fidelity.Libc.IO.open file oflag
        if result >= 0 then Ok result
        else Error (captureErrno ())

    /// C signature: int openat(int __fd, const char * __file, int __oflag)
    let openat (fd: int) (file: option<nativeptr<byte>>) (oflag: int) : Result<int, string> =
        let result = Fidelity.Libc.IO.openat fd file oflag
        if result >= 0 then Ok result
        else Error (captureErrno ())

    /// C signature: int ioctl(int __fd, unsigned long __request)
    let ioctl (fd: int) (request: uint64) : Result<int, string> =
        let result = Fidelity.Libc.IO.ioctl fd request
        if result >= 0 then Ok result
        else Error (captureErrno ())
