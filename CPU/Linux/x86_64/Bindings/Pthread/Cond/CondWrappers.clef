module Fidelity.Pthread.Cond.Wrappers

// Generated by Farscape — pthread

open Fidelity.Pthread.Cond
open Fidelity.Pthread.Errno
open Microsoft.FSharp.NativeInterop

    /// Capture errno and build CError with description from header comments.
    let captureError () : CError =
        let code = NativePtr.read (Fidelity.Pthread.Cond.__errno_location ())
        { Code = code; Description = Errno.describe code }



    /// Initialize condition variable COND using attributes ATTR, or use the default values if later is NULL.
    /// 
    /// C signature: int pthread_cond_init(pthread_cond_t *restrict __cond, const pthread_condattr_t *restrict __cond_attr)
    let pthread_cond_init (cond: nativeint) (cond_attr: nativeint) : Result<unit, CError> =
        let result = Fidelity.Pthread.Cond.pthread_cond_init cond cond_attr
        if result = 0l then Ok ()
        else Error (captureError ())

    /// Destroy condition variable COND.
    /// 
    /// C signature: int pthread_cond_destroy(pthread_cond_t * __cond)
    let pthread_cond_destroy (cond: nativeint) : Result<unit, CError> =
        let result = Fidelity.Pthread.Cond.pthread_cond_destroy cond
        if result = 0l then Ok ()
        else Error (captureError ())

    /// Wake up one thread waiting for condition variable COND.
    /// 
    /// C signature: int pthread_cond_signal(pthread_cond_t * __cond)
    let pthread_cond_signal (cond: nativeint) : Result<unit, CError> =
        let result = Fidelity.Pthread.Cond.pthread_cond_signal cond
        if result = 0l then Ok ()
        else Error (captureError ())

    /// Wake up all threads waiting for condition variables COND.
    /// 
    /// C signature: int pthread_cond_broadcast(pthread_cond_t * __cond)
    let pthread_cond_broadcast (cond: nativeint) : Result<unit, CError> =
        let result = Fidelity.Pthread.Cond.pthread_cond_broadcast cond
        if result = 0l then Ok ()
        else Error (captureError ())

    /// Wait for condition variable COND to be signaled or broadcast. MUTEX is assumed to be locked before. This function is a cancellation point and therefore not marked with __THROW.
    /// 
    /// C signature: int pthread_cond_wait(pthread_cond_t *restrict __cond, pthread_mutex_t *restrict __mutex)
    let pthread_cond_wait (cond: nativeint) (mutex: nativeint) : Result<unit, CError> =
        let result = Fidelity.Pthread.Cond.pthread_cond_wait cond mutex
        if result = 0l then Ok ()
        else Error (captureError ())

    /// C signature: int pthread_cond_timedwait(pthread_cond_t *restrict __cond, pthread_mutex_t *restrict __mutex, const struct timespec *restrict __abstime)
    let pthread_cond_timedwait (cond: nativeint) (mutex: nativeint) (abstime: nativeint) : Result<unit, CError> =
        let result = Fidelity.Pthread.Cond.pthread_cond_timedwait cond mutex abstime
        if result = 0l then Ok ()
        else Error (captureError ())
