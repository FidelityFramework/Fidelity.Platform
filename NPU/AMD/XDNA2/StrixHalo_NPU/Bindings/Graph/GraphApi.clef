module Fidelity.XRT.Graph.Api

// Generated by Farscape — xrt_coreutil

open Fidelity.XRT.Graph
open Fidelity.XRT.ReturnCode
open Fidelity.XRT.ReturnCode.ReturnCode

    /// Capture Xrt return code as human-readable error string.
    let captureReturnCode (code: int32) : string =
        describe code



    /// C signature: xrtGraphHandle xrtGraphOpen(xrtDeviceHandle handle, const unsigned char * xclbinUUID, const char * graphName)
    let xrtGraphOpen (handle: nativeint) (xclbinUUID: option<nativeptr<byte>>) (graphName: option<nativeptr<byte>>) : Result<nativeint, string> =
        match Fidelity.XRT.Graph.xrtGraphOpen handle xclbinUUID graphName with
        | Some result -> Ok result
        | None -> Error "xrtGraphOpen returned null handle"

    /// C signature: xrtGraphHandle xrtGraphOpenExclusive(xrtDeviceHandle handle, const unsigned char * xclbinUUID, const char * graphName)
    let xrtGraphOpenExclusive (handle: nativeint) (xclbinUUID: option<nativeptr<byte>>) (graphName: option<nativeptr<byte>>) : Result<nativeint, string> =
        match Fidelity.XRT.Graph.xrtGraphOpenExclusive handle xclbinUUID graphName with
        | Some result -> Ok result
        | None -> Error "xrtGraphOpenExclusive returned null handle"

    /// C signature: xrtGraphHandle xrtGraphOpenShared(xrtDeviceHandle handle, const unsigned char * xclbinUUID, const char * graphName)
    let xrtGraphOpenShared (handle: nativeint) (xclbinUUID: option<nativeptr<byte>>) (graphName: option<nativeptr<byte>>) : Result<nativeint, string> =
        match Fidelity.XRT.Graph.xrtGraphOpenShared handle xclbinUUID graphName with
        | Some result -> Ok result
        | None -> Error "xrtGraphOpenShared returned null handle"

    /// C signature: void xrtGraphClose(xrtGraphHandle gh)
    let xrtGraphClose (gh: nativeint) : unit =
        Fidelity.XRT.Graph.xrtGraphClose gh

    /// C signature: int xrtGraphReset(xrtGraphHandle gh)
    let xrtGraphReset (gh: nativeint) : Result<unit, string> =
        let result = Fidelity.XRT.Graph.xrtGraphReset gh
        if result = 0 then Ok ()
        else Error (captureReturnCode (int32 (result)))

    /// C signature: uint64_t xrtGraphTimeStamp(xrtGraphHandle gh)
    let xrtGraphTimeStamp (gh: nativeint) : uint64 =
        Fidelity.XRT.Graph.xrtGraphTimeStamp gh

    /// C signature: int xrtGraphRun(xrtGraphHandle gh, int iterations)
    let xrtGraphRun (gh: nativeint) (iterations: int) : Result<unit, string> =
        let result = Fidelity.XRT.Graph.xrtGraphRun gh iterations
        if result = 0 then Ok ()
        else Error (captureReturnCode (int32 (result)))

    /// C signature: int xrtGraphWaitDone(xrtGraphHandle gh, int timeoutMilliSec)
    let xrtGraphWaitDone (gh: nativeint) (timeoutMilliSec: int) : Result<unit, string> =
        let result = Fidelity.XRT.Graph.xrtGraphWaitDone gh timeoutMilliSec
        if result = 0 then Ok ()
        else Error (captureReturnCode (int32 (result)))

    /// C signature: int xrtGraphWait(xrtGraphHandle gh, uint64_t cycle)
    let xrtGraphWait (gh: nativeint) (cycle: uint64) : Result<unit, string> =
        let result = Fidelity.XRT.Graph.xrtGraphWait gh cycle
        if result = 0 then Ok ()
        else Error (captureReturnCode (int32 (result)))

    /// C signature: int xrtGraphSuspend(xrtGraphHandle gh)
    let xrtGraphSuspend (gh: nativeint) : Result<unit, string> =
        let result = Fidelity.XRT.Graph.xrtGraphSuspend gh
        if result = 0 then Ok ()
        else Error (captureReturnCode (int32 (result)))

    /// C signature: int xrtGraphResume(xrtGraphHandle gh)
    let xrtGraphResume (gh: nativeint) : Result<unit, string> =
        let result = Fidelity.XRT.Graph.xrtGraphResume gh
        if result = 0 then Ok ()
        else Error (captureReturnCode (int32 (result)))

    /// C signature: int xrtGraphEnd(xrtGraphHandle gh, uint64_t cycle)
    let xrtGraphEnd (gh: nativeint) (cycle: uint64) : Result<unit, string> =
        let result = Fidelity.XRT.Graph.xrtGraphEnd gh cycle
        if result = 0 then Ok ()
        else Error (captureReturnCode (int32 (result)))

    /// C signature: int xrtGraphUpdateRTP(xrtGraphHandle gh, const char * hierPathPort, const char * buffer, size_t size)
    let xrtGraphUpdateRTP (gh: nativeint) (hierPathPort: option<nativeptr<byte>>) (buffer: option<nativeptr<byte>>) (size: unativeint) : Result<unit, string> =
        let result = Fidelity.XRT.Graph.xrtGraphUpdateRTP gh hierPathPort buffer size
        if result = 0 then Ok ()
        else Error (captureReturnCode (int32 (result)))

    /// C signature: int xrtGraphReadRTP(xrtGraphHandle gh, const char * hierPathPort, char * buffer, size_t size)
    let xrtGraphReadRTP (gh: nativeint) (hierPathPort: option<nativeptr<byte>>) (buffer: option<nativeptr<byte>>) (size: unativeint) : Result<unit, string> =
        let result = Fidelity.XRT.Graph.xrtGraphReadRTP gh hierPathPort buffer size
        if result = 0 then Ok ()
        else Error (captureReturnCode (int32 (result)))
