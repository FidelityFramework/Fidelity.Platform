module Fidelity.Libc.Socket.Api

// Generated by Farscape — c

open Fidelity.Libc.Socket
open Fidelity.Libc.Errno
open Fidelity.Libc.Errno.Errno

    /// Capture errno as human-readable description string from header comments.
    let captureErrno () : string =
        describe (NativePtr.read (NativePtr.ofNativeInt (__errno_location ())))



    /// Create a new socket of type TYPE in domain DOMAIN, using protocol PROTOCOL.  If PROTOCOL is zero, one is chosen automatically. Returns a file descriptor for the new socket, or -1 for errors.
    /// 
    /// C signature: int socket(int __domain, int __type, int __protocol)
    let socket (domain: int) (``type``: int) (protocol: int) : Result<int, string> =
        let result = Fidelity.Libc.Socket.socket domain ``type`` protocol
        if result >= 0 then Ok result
        else Error (captureErrno ())

    /// Create two new sockets, of type TYPE in domain DOMAIN and using protocol PROTOCOL, which are connected to each other, and put file descriptors for them in FDS[0] and FDS[1].  If PROTOCOL is zero, one will be chosen automatically.  Returns 0 on success, -1 for errors.
    /// 
    /// C signature: int socketpair(int __domain, int __type, int __protocol, int * __fds)
    let socketpair (domain: int) (``type``: int) (protocol: int) (fds: option<nativeint>) : Result<unit, string> =
        let result = Fidelity.Libc.Socket.socketpair domain ``type`` protocol fds
        if result = 0 then Ok ()
        else Error (captureErrno ())

    /// Give the socket FD the local address ADDR (which is LEN bytes long).
    /// 
    /// C signature: int bind(int __fd, const struct sockaddr * __addr, socklen_t __len)
    let bind (fd: int) (addr: option<nativeint>) (len: uint) : Result<unit, string> =
        let result = Fidelity.Libc.Socket.bind fd addr len
        if result = 0 then Ok ()
        else Error (captureErrno ())

    /// Put the local address of FD into *ADDR and its length in *LEN.
    /// 
    /// C signature: int getsockname(int __fd, struct sockaddr *restrict __addr, socklen_t *restrict __len)
    let getsockname (fd: int) (addr: option<nativeint>) (len: option<nativeint>) : Result<unit, string> =
        let result = Fidelity.Libc.Socket.getsockname fd addr len
        if result = 0 then Ok ()
        else Error (captureErrno ())

    /// Open a connection on socket FD to peer at ADDR (which LEN bytes long). For connectionless socket types, just set the default address to send to and the only address from which to accept transmissions. Return 0 on success, -1 for errors. This function is a cancellation point and therefore not marked with __THROW.
    /// 
    /// C signature: int connect(int __fd, const struct sockaddr * __addr, socklen_t __len)
    let connect (fd: int) (addr: option<nativeint>) (len: uint) : Result<unit, string> =
        let result = Fidelity.Libc.Socket.connect fd addr len
        if result = 0 then Ok ()
        else Error (captureErrno ())

    /// Put the address of the peer connected to socket FD into *ADDR (which is *LEN bytes long), and its actual length into *LEN.
    /// 
    /// C signature: int getpeername(int __fd, struct sockaddr *restrict __addr, socklen_t *restrict __len)
    let getpeername (fd: int) (addr: option<nativeint>) (len: option<nativeint>) : Result<unit, string> =
        let result = Fidelity.Libc.Socket.getpeername fd addr len
        if result = 0 then Ok ()
        else Error (captureErrno ())

    /// Send N bytes of BUF to socket FD.  Returns the number sent or -1. This function is a cancellation point and therefore not marked with __THROW.
    /// 
    /// C signature: ssize_t send(int __fd, const void * __buf, size_t __n, int __flags)
    let send (fd: int) (buf: option<nativeint>) (n: unativeint) (flags: int) : Result<nativeint, string> =
        let result = Fidelity.Libc.Socket.send fd buf n flags
        if result >= 0n then Ok result
        else Error (captureErrno ())

    /// Read N bytes into BUF from socket FD. Returns the number read or -1 for errors. This function is a cancellation point and therefore not marked with __THROW.
    /// 
    /// C signature: ssize_t recv(int __fd, void * __buf, size_t __n, int __flags)
    let recv (fd: int) (buf: option<nativeint>) (n: unativeint) (flags: int) : Result<nativeint, string> =
        let result = Fidelity.Libc.Socket.recv fd buf n flags
        if result >= 0n then Ok result
        else Error (captureErrno ())

    /// Send N bytes of BUF on socket FD to peer at address ADDR (which is ADDR_LEN bytes long).  Returns the number sent, or -1 for errors. This function is a cancellation point and therefore not marked with __THROW.
    /// 
    /// C signature: ssize_t sendto(int __fd, const void * __buf, size_t __n, int __flags, const struct sockaddr * __addr, socklen_t __addr_len)
    let sendto (fd: int) (buf: option<nativeint>) (n: unativeint) (flags: int) (addr: option<nativeint>) (addr_len: uint) : Result<nativeint, string> =
        let result = Fidelity.Libc.Socket.sendto fd buf n flags addr addr_len
        if result >= 0n then Ok result
        else Error (captureErrno ())

    /// Read N bytes into BUF through socket FD. If ADDR is not NULL, fill in *ADDR_LEN bytes of it with the address of the sender, and store the actual size of the address in *ADDR_LEN. Returns the number of bytes read or -1 for errors. This function is a cancellation point and therefore not marked with __THROW.
    /// 
    /// C signature: ssize_t recvfrom(int __fd, void *restrict __buf, size_t __n, int __flags, struct sockaddr *restrict __addr, socklen_t *restrict __addr_len)
    let recvfrom (fd: int) (buf: option<nativeint>) (n: unativeint) (flags: int) (addr: option<nativeint>) (addr_len: option<nativeint>) : Result<nativeint, string> =
        let result = Fidelity.Libc.Socket.recvfrom fd buf n flags addr addr_len
        if result >= 0n then Ok result
        else Error (captureErrno ())

    /// C signature: ssize_t sendmsg(int __fd, const struct msghdr * __message, int __flags)
    let sendmsg (fd: int) (message: option<nativeint>) (flags: int) : Result<nativeint, string> =
        let result = Fidelity.Libc.Socket.sendmsg fd message flags
        if result >= 0n then Ok result
        else Error (captureErrno ())

    /// C signature: ssize_t recvmsg(int __fd, struct msghdr * __message, int __flags)
    let recvmsg (fd: int) (message: option<nativeint>) (flags: int) : Result<nativeint, string> =
        let result = Fidelity.Libc.Socket.recvmsg fd message flags
        if result >= 0n then Ok result
        else Error (captureErrno ())

    /// C signature: int getsockopt(int __fd, int __level, int __optname, void *restrict __optval, socklen_t *restrict __optlen)
    let getsockopt (fd: int) (level: int) (optname: int) (optval: option<nativeint>) (optlen: option<nativeint>) : Result<unit, string> =
        let result = Fidelity.Libc.Socket.getsockopt fd level optname optval optlen
        if result = 0 then Ok ()
        else Error (captureErrno ())

    /// C signature: int setsockopt(int __fd, int __level, int __optname, const void * __optval, socklen_t __optlen)
    let setsockopt (fd: int) (level: int) (optname: int) (optval: option<nativeint>) (optlen: uint) : Result<unit, string> =
        let result = Fidelity.Libc.Socket.setsockopt fd level optname optval optlen
        if result = 0 then Ok ()
        else Error (captureErrno ())

    /// Prepare to accept connections on socket FD. N connection requests will be queued before further requests are refused. Returns 0 on success, -1 for errors.
    /// 
    /// C signature: int listen(int __fd, int __n)
    let listen (fd: int) (n: int) : Result<unit, string> =
        let result = Fidelity.Libc.Socket.listen fd n
        if result = 0 then Ok ()
        else Error (captureErrno ())

    /// Await a connection on socket FD. When a connection arrives, open a new socket to communicate with it, set *ADDR (which is *ADDR_LEN bytes long) to the address of the connecting peer and *ADDR_LEN to the address's actual length, and return the new socket's descriptor, or -1 for errors. This function is a cancellation point and therefore not marked with __THROW.
    /// 
    /// C signature: int accept(int __fd, struct sockaddr *restrict __addr, socklen_t *restrict __addr_len)
    let accept (fd: int) (addr: option<nativeint>) (addr_len: option<nativeint>) : Result<int, string> =
        let result = Fidelity.Libc.Socket.accept fd addr addr_len
        if result >= 0 then Ok result
        else Error (captureErrno ())

    /// Shut down all or part of the connection open on socket FD. HOW determines what to shut down: SHUT_RD   = No more receptions; SHUT_WR   = No more transmissions; SHUT_RDWR = No more receptions or transmissions. Returns 0 on success, -1 for errors.
    /// 
    /// C signature: int shutdown(int __fd, int __how)
    let shutdown (fd: int) (how: int) : Result<unit, string> =
        let result = Fidelity.Libc.Socket.shutdown fd how
        if result = 0 then Ok ()
        else Error (captureErrno ())

    /// C signature: int poll(struct pollfd * __fds, nfds_t __nfds, int __timeout)
    let poll (fds: option<nativeint>) (nfds: uint64) (timeout: int) : Result<int, string> =
        let result = Fidelity.Libc.Socket.poll fds nfds timeout
        if result >= 0 then Ok result
        else Error (captureErrno ())
